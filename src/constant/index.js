/**
 * Exporting all constants from this folder
 */
const ACTIVITY_TYPES = require('./activity-types');
const SUCCESS_CODES = require('./success-message');
const ERROR_CODES = require('./error-messages');
const ENUM = require('./enum');
const ROLES = require('./roles');

module.exports = {
  ACTIVITY_TYPES,
  SUCCESS_CODES,
  ERROR_CODES,
  ENUM,
  ROLES,
};
